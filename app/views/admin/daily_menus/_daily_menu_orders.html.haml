.ui.segment
  %table.ui.very.basic.table
    %thead
      %tr 
        %th User name 
        %th Menu items
        %th Total cost
      %tbody
        - @facade.menu_orders.each do |order|
          %tr
            %td 
              = link_to order.user_name, admin_user_path(order.user)
            %td 
              = order.food_items_humanized
            %td  
              = order.cost
              