- if page.current?
  = link_to url, title: page, class: 'item active', remote: remote do
    = @facade.paginated_daily_menus.navigation_dates
- else
  = link_to url, title: page, class: 'item', remote: remote do
    = page
